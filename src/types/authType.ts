export type Field = {
  label: string;
  type: string;
  name: string;
  value: any;
};
export type RegisterData = {
  name: string;
  email: string;
  password: string;
};
export type LoginData = {
  email: string;
  password: string;
};
